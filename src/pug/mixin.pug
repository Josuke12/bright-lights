mixin createTitle(title)
	.title
		//-
			.music-visualization
			TODO: Если будет время, то сделать визуализацию звука не как картинку, а через JS
		img(src="img/audio-visual.svg", alt="")
		h2=title
		- ;


mixin createPlayer()
	.player
		img(src="img/play.svg", alt="", class="play")
		.progress_container
			.progress
			.progress_btn
		.player__time
			span.current-time 00:00
			span -
			span.max-time 00:00


mixin createList(...element)
	//- Миксин, который позволяет быстро создать список с неограниченным количеством элементов
	ul(class=attributes.class)
		each liItem in element
			li!= liItem